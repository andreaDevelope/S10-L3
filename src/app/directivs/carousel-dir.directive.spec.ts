import { CarouselDirDirective } from './carousel-dir.directive';

describe('CarouselDirDirective', () => {
  it('should create an instance', () => {
    const directive = new CarouselDirDirective();
    expect(directive).toBeTruthy();
  });
});
